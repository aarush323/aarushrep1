#include <iostream>

int main(){
    using std::cout;
    double pi = 3.1416, r = 10, circumference;
    circumference = 2* pi *r;
    cout<< circumference << "cm";

// z = std::max(x,y);
//z = std::min(x,y);
// z = pow(2,4) = 16;
// z = sqrt (9);
// z = abs(-3);
// z = round(x);
//z = ceil(x); round up
// z = floor(x); round dowm




}